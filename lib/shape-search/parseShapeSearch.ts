import { GeneralComponent } from "../interaction/container";

/**
 * shape search regex to component
 * @param expr 
 * @returns 
 */
export default function parseShapeSearch(regex: string): GeneralComponent | null{
  return null;
}